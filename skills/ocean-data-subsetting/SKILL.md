---
name: ocean-data-subsetting
description: 规划海洋数据变量、区域、深度、时间和输出格式时使用。
---

# 海洋数据切片规程

1. 在下载前明确数据源、数据集、变量、经纬度、深度、时间和输出格式。
2. 先执行只读预检，展示估计体积、目标 URI、认证状态和估算方法。
3. 不把变量名、单位、维度或数据覆盖范围当作已知事实；缺少元数据时要求探测。
4. 经度跨越日期变更线、深度正负约定和时间日历必须显式检查。
5. 下载、计算和外部写入必须经过审批；Skill 本身不得绕过审批门。
